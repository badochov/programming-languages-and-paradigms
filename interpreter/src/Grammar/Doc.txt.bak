The Language grammar
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of grammar==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===
Integer literals //Integer// are nonempty sequences of digits.






VarIdent literals are recognized by the regular expression
`````lower ('_' | digit | letter)*`````

TypeIdent literals are recognized by the regular expression
`````upper ('_' | digit | letter)*`````

PolyIdentToken literals are recognized by the regular expression
`````lower`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in grammar are the following:
  | ``bool`` | ``else`` | ``if`` | ``in``
  | ``int`` | ``let`` | ``match`` | ``then``
  | ``type`` | ``with`` |  |

The symbols used in grammar are the following:
  | = | ( | ) | ;
  | | | ' | [ | ]
  | -> | ! | && | \/
  | - | : | , | _
  | [] | + | * | /
  | % | < | <= | >
  | >= | == | != |

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of grammar==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //[TopDef]//
  | //TopDef// | -> | //VarDef//
  |  |  **|**  | //TypeDef//
  | //TypeDef// | -> | ``type`` //TypeIdent// ``=`` //TypeDefOption//
  | //TypeDefOption// | -> | //TypeIdent// //[TypeH]//
  | //TypeH// | -> | ``(`` //TypeDefOption// ``)``
  |  |  **|**  | //Type//
  | //VarDef// | -> | //VarIdent// ``=`` //Expr//
  | //[TopDef]// | -> | //TopDef//
  |  |  **|**  | //TopDef// ``;`` //[TopDef]//
  | //Arg// | -> | //VarIdent//
  | //[Arg]// | -> | **eps**
  |  |  **|**  | //Arg// //[Arg]//
  | //[TypeDefOption]// | -> | **eps**
  |  |  **|**  | //TypeDefOption//
  |  |  **|**  | //TypeDefOption// ``|`` //[TypeDefOption]//
  | //[TypeH]// | -> | **eps**
  |  |  **|**  | //TypeH// //[TypeH]//
  | //Type// | -> | ``int``
  |  |  **|**  | ``bool``
  |  |  **|**  | ``'`` //PolyIdentToken//
  |  |  **|**  | //[TypeFnH]//
  |  |  **|**  | ``[`` //Type// ``]``
  |  |  **|**  | //TypeIdent// //[PolyIdent]//
  | //TypeFnH// | -> | //Type//
  | //PolyIdent// | -> | //Type//
  | //[PolyIdent]// | -> | **eps**
  |  |  **|**  | //PolyIdent// //[PolyIdent]//
  | //[TypeFnH]// | -> | **eps**
  |  |  **|**  | //TypeFnH//
  |  |  **|**  | //TypeFnH// ``->`` //[TypeFnH]//
  | //BExpr4// | -> | //VarIdent// //[Expr]//
  |  |  **|**  | ``(`` //BExpr// ``)``
  |  |  **|**  | ``(`` //BExpr// ``)``
  | //BExpr3// | -> | ``!`` //BExpr4//
  |  |  **|**  | //BExpr4//
  | //BExpr2// | -> | //Expr// //RelOp// //Expr//
  |  |  **|**  | //BExpr3//
  | //BExpr1// | -> | //BExpr2// ``&&`` //BExpr1//
  |  |  **|**  | //BExpr2//
  | //BExpr// | -> | //BExpr1// ``\/`` //BExpr//
  |  |  **|**  | //BExpr1//
  | //Expr3// | -> | ``|`` //[Arg]// ``|`` //Expr//
  |  |  **|**  | //Match//
  |  |  **|**  | //BExpr//
  |  |  **|**  | ``let`` //VarDef// ``in`` //Expr//
  |  |  **|**  | ``if`` //BExpr// ``then`` //Expr// ``else`` //Expr//
  |  |  **|**  | //Ident// //[Expr]//
  |  |  **|**  | //Integer//
  |  |  **|**  | ``[`` //[LArg]// ``]``
  |  |  **|**  | ``(`` //Expr// ``)``
  |  |  **|**  | ``(`` //Expr// ``)``
  | //Expr2// | -> | ``-`` //Expr3//
  |  |  **|**  | //Expr3// ``:`` //Expr3//
  |  |  **|**  | //Expr3//
  | //Expr1// | -> | //Expr1// //MulOp// //Expr2//
  |  |  **|**  | //Expr2//
  | //Expr// | -> | //Expr1// //AddOp// //Expr//
  |  |  **|**  | //Expr1//
  | //LArg// | -> | //Expr//
  | //[LArg]// | -> | **eps**
  |  |  **|**  | //LArg//
  |  |  **|**  | //LArg// ``,`` //[LArg]//
  | //[Expr]// | -> | **eps**
  |  |  **|**  | //Expr// //[Expr]//
  | //Match// | -> | ``match`` //Expr// ``with`` //[MatchArm]//
  | //MatchArm// | -> | //MatchArmSpecifier// ``->`` //Expr//
  | //MatchArmSpecifierHelper// | -> | //MatchArmSpecifier//
  |  |  **|**  | //VarIdent//
  |  |  **|**  | ``_``
  | //MatchArmSpecifier// | -> | ``[]``
  |  |  **|**  | ``[`` //MatchArmSpecifierHelper// ``]``
  |  |  **|**  | //MatchArmSpecifierHelper// ``:`` //MatchArmSpecifierHelper//
  |  |  **|**  | //TypeIdent// //[MatchArmTypeHelper]//
  | //MatchArmTypeHelper// | -> | //TypeIdent//
  |  |  **|**  | ``_``
  |  |  **|**  | ``(`` //MatchArmSpecifier// ``)``
  | //[MatchArm]// | -> | **eps**
  |  |  **|**  | //MatchArm// //[MatchArm]//
  | //[MatchArmTypeHelper]// | -> | **eps**
  |  |  **|**  | //MatchArmTypeHelper// //[MatchArmTypeHelper]//
  | //AddOp// | -> | ``+``
  |  |  **|**  | ``-``
  | //MulOp// | -> | ``*``
  |  |  **|**  | ``/``
  |  |  **|**  | ``%``
  | //RelOp// | -> | ``<``
  |  |  **|**  | ``<=``
  |  |  **|**  | ``>``
  |  |  **|**  | ``>=``
  |  |  **|**  | ``==``
  |  |  **|**  | ``!=``



%% File generated by the BNF Converter (bnfc 2.9.4).
