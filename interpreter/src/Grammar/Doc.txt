The Language grammar
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of grammar==

===Literals===
Integer literals //Integer// are nonempty sequences of digits.






VarName literals are recognized by the regular expression
`````lower ('_' | digit | letter)*`````

TypeName literals are recognized by the regular expression
`````upper ('_' | digit | letter)*`````

PolyIdentToken literals are recognized by the regular expression
`````lower`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in grammar are the following:
  | ``bool`` | ``else`` | ``if`` | ``in``
  | ``int`` | ``let`` | ``match`` | ``then``
  | ``type`` | ``with`` |  |

The symbols used in grammar are the following:
  | ; | = | ( | )
  | | | ' | [ | ]
  | -> | - | ! | :
  | && | \/ | , | _
  | [] | + | * | /
  | % | < | <= | >
  | >= | == | != |

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of grammar==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //[TopDef]//
  | //TopDef// | -> | //VarDef// ``;``
  |  |  **|**  | ``type`` //TypeName// ``=`` //[TypeDefOption]// ``;``
  | //TypeDefOption// | -> | //TypeName// //[TypeH]//
  | //TypeH// | -> | ``(`` //TypeDefOption// ``)``
  |  |  **|**  | //Type//
  | //VarDef// | -> | //VarName// ``=`` //Expr//
  | //[TopDef]// | -> | //TopDef//
  |  |  **|**  | //TopDef// //[TopDef]//
  | //[VarName]// | -> | **eps**
  |  |  **|**  | //VarName// //[VarName]//
  | //[TypeDefOption]// | -> | **eps**
  |  |  **|**  | //TypeDefOption//
  |  |  **|**  | //TypeDefOption// ``|`` //[TypeDefOption]//
  | //[TypeH]// | -> | **eps**
  |  |  **|**  | //TypeH// //[TypeH]//
  | //Type// | -> | ``int``
  |  |  **|**  | ``bool``
  |  |  **|**  | ``'`` //PolyIdentToken//
  |  |  **|**  | ``(`` //[TypeFnH]// ``)``
  |  |  **|**  | ``[`` //Type// ``]``
  |  |  **|**  | //TypeName// //[PolyIdent]//
  | //TypeFnH// | -> | //Type//
  | //PolyIdent// | -> | //Type//
  | //[PolyIdent]// | -> | //PolyIdent//
  |  |  **|**  | //PolyIdent// //[PolyIdent]//
  | //[TypeFnH]// | -> | //TypeFnH//
  |  |  **|**  | //TypeFnH// ``->`` //[TypeFnH]//
  | //Expr6// | -> | ``|`` //[VarName]// ``|`` //Expr//
  |  |  **|**  | //Match//
  |  |  **|**  | ``let`` //VarDef// ``in`` //Expr//
  |  |  **|**  | ``if`` //Expr// ``then`` //Expr// ``else`` //Expr//
  |  |  **|**  | //VarOrTypeIdent// //[Expr]//
  |  |  **|**  | //Integer//
  |  |  **|**  | ``[`` //[ListArg]// ``]``
  |  |  **|**  | ``(`` //Expr// ``)``
  |  |  **|**  | ``(`` //Expr// ``)``
  | //Expr5// | -> | ``-`` //Expr6//
  |  |  **|**  | ``!`` //Expr6//
  |  |  **|**  | //Expr6// ``:`` //Expr6//
  |  |  **|**  | //Expr6//
  | //Expr4// | -> | //Expr4// //MulOp// //Expr5//
  |  |  **|**  | //Expr5//
  | //Expr3// | -> | //Expr3// //AddOp// //Expr4//
  |  |  **|**  | //Expr4//
  | //Expr2// | -> | //Expr2// //RelOp// //Expr3//
  |  |  **|**  | //Expr3//
  | //Expr1// | -> | //Expr2// ``&&`` //Expr1//
  |  |  **|**  | //Expr2//
  | //Expr// | -> | //Expr1// ``\/`` //Expr//
  |  |  **|**  | //Expr1//
  | //ListArg// | -> | //Expr//
  | //VarOrTypeIdent// | -> | //VarName//
  |  |  **|**  | //TypeName//
  | //[ListArg]// | -> | **eps**
  |  |  **|**  | //ListArg//
  |  |  **|**  | //ListArg// ``,`` //[ListArg]//
  | //[Expr]// | -> | **eps**
  |  |  **|**  | //Expr// //[Expr]//
  | //Match// | -> | ``match`` //Expr// ``with`` //[MatchArm]//
  | //MatchArm// | -> | //MatchArmSpecifier// ``->`` //Expr//
  | //MatchArmSpecifierHelper// | -> | //MatchArmSpecifier//
  |  |  **|**  | //VarName//
  |  |  **|**  | ``_``
  | //MatchArmSpecifier// | -> | ``[]``
  |  |  **|**  | ``[`` //MatchArmSpecifierHelper// ``]``
  |  |  **|**  | //MatchArmSpecifierHelper// ``:`` //MatchArmSpecifierHelper//
  |  |  **|**  | //TypeName// //[MatchArmTypeHelper]//
  | //MatchArmTypeHelper// | -> | //TypeName//
  |  |  **|**  | ``_``
  |  |  **|**  | ``(`` //MatchArmSpecifier// ``)``
  | //[MatchArm]// | -> | **eps**
  |  |  **|**  | //MatchArm// //[MatchArm]//
  | //[MatchArmTypeHelper]// | -> | **eps**
  |  |  **|**  | //MatchArmTypeHelper// //[MatchArmTypeHelper]//
  | //AddOp// | -> | ``+``
  |  |  **|**  | ``-``
  | //MulOp// | -> | ``*``
  |  |  **|**  | ``/``
  |  |  **|**  | ``%``
  | //RelOp// | -> | ``<``
  |  |  **|**  | ``<=``
  |  |  **|**  | ``>``
  |  |  **|**  | ``>=``
  |  |  **|**  | ``==``
  |  |  **|**  | ``!=``



%% File generated by the BNF Converter (bnfc 2.9.4).
